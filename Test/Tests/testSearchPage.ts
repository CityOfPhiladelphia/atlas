import Searchpage from "../Pages/SearchPage";

fixture`search page verification`.page`https://atlas.phila.gov/`;

let searchpage = new Searchpage();

test("search page verification", async (t: TestController) => {
  // await searchpage.verifySearchFunctionality(t);
  // await searchpage.verifyTopicPropertyAssesment(t);
  // await searchpage.verifyTopicDeeds(t);
  // await searchpage.verifyTopicLicensesAndInspection(t)
  // await searchpage.verifyTopicZoning(t);
  // await searchpage.verifyTopicVoting(t);
  // await searchpage.verifyTopicNearby(t);
});
